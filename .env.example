# Firebase web app config (Project Settings -> General -> Your apps -> SDK setup)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_APP_ID=

# Anthropic API key (console.anthropic.com). WARNING: this is exposed in the
# client bundle — see the security note in the README before sharing the app URL.
VITE_ANTHROPIC_API_KEY=
